##
## Banking configuration (per-platform)
##
## Phase B1 of the cross-platform plan externalises the banking constants
## that the build tools and the engine used to hard-code. New platforms
## (e.g. CPC) add their own sub-key under `banking:`; ZX 128 keeps its
## historic values verbatim.
##
## Fields (per platform):
##   dataset_valid_banks
##     Ordered list of physical RAM banks where datasets may be placed.
##     The order encodes the storage preference used by `banktool.pl`
##     when laying out datasets (first entry tried first). For ZX 128
##     the contended banks come first so non-contended banks remain
##     available for code that benefits from no-wait-state access.
##   codeset_valid_banks
##     Ordered list of physical RAM banks where codesets may be placed.
##     Same ordering semantics as `dataset_valid_banks` but inverted:
##     non-contended banks come first because codesets execute code and
##     benefit most from being uncontended.
##   engine_code_memory_bank
##     RAM bank that holds the engine's banked-function table and the
##     engine banked code. Switched in at `swap_window` whenever the
##     trampolines in `engine/src/00lowmem.c` are entered. Must NOT
##     appear in `codeset_valid_banks` (codesets use a different bank).
##   swap_window
##     Z80 address at which the bank-switched 16K window appears. For
##     ZX 128 this is 0xC000 (the upper 16K page). The banked-function
##     table and the codeset_assets_s struct are both anchored at this
##     address.
##
banking:
  zx128:
    dataset_valid_banks:    [ 1, 3, 7, 6, 4 ]
    codeset_valid_banks:    [ 6, 1, 3, 7 ]
    engine_code_memory_bank: 4
    swap_window:             0xC000

##
## build configuration
##
build:
  generated_dir: 'build/generated'
  banked_functions:
    asm_table_filename: 'build/generated/banked/128/00banked_function_table.asm'
    c_macros_filename: 'build/generated/banked_function_defs.h'
  
##
## tools configuration
##
tools:
  arkos:
    dir: '/home/jorgegv/src/spectrum/arkos/Arkos Tracker 2'

##
## IM2 interrupt configuration (per-platform)
##
## Phase B2 of the cross-platform plan reorganises this section under a
## nested `interrupts:` tree so other platforms (e.g. CPC) add their own
## sub-key as a sibling. ZX 128 keeps its historic values verbatim.
##
## For backwards compatibility, the legacy flat key `interrupts_128:` is
## kept as a permanent silent alias at the top level (per README §5.6).
## It is wired via a YAML anchor (&interrupts_zx128) so the two spellings
## point at the same data — readers may use either spelling indefinitely.
## No removal is scheduled.
##
## Int config is fixed for 48K mode.
##
## 257 bytes with value ISR_VECTOR_BYTE are stored at address IV_TABLE_ADDR
## ISR address is built as 2 consecutive bytes of value ISR_VECTOR_BYTE, e.g. 0x8181
## At that address there is a JP <ISR> 3-byte jump instruction to the real ISR routine.
## So the real code starts at ISR address + 3.
##

# 128K interrupt map — used by both sprite engines (SP1 and JSP). The IV
# table, ISR and stack live at the bottom of memory; only the top-of-memory
# sprite-data layout differs between engines, not the interrupt config.
interrupts:
  zx128: &interrupts_zx128
    iv_table_addr: 0x8000
    isr_vector_byte: 0x81
    isr_vector_address: 0x8181
    base_code_address: 0x8184

# Legacy flat alias — permanent (README §5.6). All existing tools that
# read `interrupts_128:` keep working unchanged.
interrupts_128: *interrupts_zx128

#
# alternative map with IV at 0x8400, ISR at 0x8585 and code starting at 0x8588
#
#interrupts:
#  zx128: &interrupts_zx128
#    iv_table_addr: 0x8400
#    isr_vector_byte: 0x85
#    isr_vector_address: 0x8585
#    base_code_address: 0x8588
#interrupts_128: *interrupts_zx128

##
## banked functions database
##
## Fields:
##   name (mandatory)
##   signature (optional): a16, a16_r8, etc.
##   build_dependency (optional): BUILD_FEATURE_xx
##
## Definitions for the given functions will only be output if
## BUILD_FEATURE_xx is enabled in features.h, or no build_dependency is
## present
##
## See doc/BANKED-FUNCTIONS.md for details
##
banked_functions:

  # init_main_shared_data
  - name: init_main_shared_data
    signature: a16

  # beeper
  - name: beeper_play_pending_fx
  - name: beeper_request_fx
    signature: a16
  - name: init_beeper
  - name: beeper_play_fx
    signature: a16

  # animation
  - name: animation_sequence_tick
    signature: a16_a8_r8
  - name: animation_reset_state
    signature: a16

  # hero
  - name: hero_animate_and_move

  # enemy
  - name: enemy_animate_and_move_all

  # bullet
  - name: bullet_animate_and_move_all
    build_dependency: BUILD_FEATURE_HERO_HAS_WEAPON
  - name: bullet_add
    build_dependency: BUILD_FEATURE_HERO_HAS_WEAPON

  # tracker
  - name: init_tracker
    build_dependency: BUILD_FEATURE_TRACKER
  - name: tracker_select_song
    signature: a16
    build_dependency: BUILD_FEATURE_TRACKER
  - name: tracker_start
    build_dependency: BUILD_FEATURE_TRACKER
  - name: tracker_stop
    build_dependency: BUILD_FEATURE_TRACKER
  - name: tracker_do_periodic_tasks
    build_dependency: BUILD_FEATURE_TRACKER
  - name: init_tracker_sound_effects
    build_dependency: BUILD_FEATURE_TRACKER_SOUNDFX
  - name: tracker_play_fx
    signature: a16
    build_dependency: BUILD_FEATURE_TRACKER_SOUNDFX
  - name: tracker_rewind
    build_dependency: BUILD_FEATURE_TRACKER
  - name: tracker_play_pending_fx
    build_dependency: BUILD_FEATURE_TRACKER_SOUNDFX
  - name: tracker_request_fx
    signature: a16
    build_dependency: BUILD_FEATURE_TRACKER_SOUNDFX
