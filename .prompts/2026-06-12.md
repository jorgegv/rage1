# Tasks

## Summary of 2026-06-11 (cpc-banked bring-up)

cpc-banked (CPC 6128) went from "doesn't boot" to a working render pipeline,
cap32-verified. 3 commits on branch cpc_banked_bringup (NOT pushed):
- 098cc53 z88dk-native banking: bankscaffoldtool.pl + loadbanks crt0 (drops the
  hand-rolled LOADER.BIN); appmake --disk emits GAME.B<n>.
- 67ae58c section-check fix (un-broke the no-engine CPC smoke games).
- a392d15 Shape A memory map (config-driven via yq) = THE render fix: JSP data
  block page C -> page B 0x5800-0x7FFF, bss -> page C, crt_org_code 0x1200->0x0200.
Result: boots, loadbanks, live IM1 interrupts, renders tiles + hero sprite.
Review CLEAN; all-test-builds 23/23. Full detail: .prompts/2026-06-11.md.

## Task 1: cpc-banked keyboard input (QAOP) — hero renders but does not move

- Carried over from 2026-06-11: the hero sprite renders on cpc-banked but does
  not respond to QAOP keys — CPC keyboard input is not working yet.
- Path: game_loop.c `check_controller()` -> `input_scan()` (CPC key-matrix scan)
  -> `controller_read_state()`. First check whether input_scan / the controller
  has a working CPC arm at all (it may be a stub).
- See session-handover.md (project memory) for full context.

## Known follow-ups (not today's task unless prioritised)

- LATENT TRAP: the page-C dataset decompress buffer is capped at 6 KB
  (0x8000-0x9800) with no build-time guard — add an assertion vs
  BUILD_MAX_DATASET_SIZE_CPC6128 <= (page_c_data_base - banked_dataset_base).
- Optional: source the remaining memory.h map constants (DATASET_LOAD_BASE etc.)
  from rage1-config.yml memory_map.cpc_banked too, for completeness.

## Task completion status

- [ ] Task 1: cpc-banked keyboard input (QAOP) — hero renders but does not move
