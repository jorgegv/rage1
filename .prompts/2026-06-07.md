# Tasks

<!--
==============================================================================
HOW TO HANDLE THIS FILE (daily-tasks skill rules — read before every edit)
==============================================================================
- ON START of a task ("do task N"): the FIRST action, before any other work,
  is to flip that task's line in "## Task completion status" to [~]. If you
  catch yourself working without having flipped it, stop and flip it now.
- WHILE WORKING: record details/findings/completion notes as bullets in that
  task's own "## Task N" section (add a "### Completion notes" sub-heading if
  helpful). NEVER put prose in the status section.
- ON FINISH: tell the user, ask for confirmation. Do NOT mark [x] yourself.
  Status stays [~] until the user explicitly confirms.
- ON CONFIRMATION: only then change [~] -> [x]. User has the last word.
- STATUS MARKS: [ ] not started | [~] ongoing | [x] done (user-confirmed only).
- "## Task completion status" holds ONLY one-line "- [<mark>] Task N: <title>"
  entries. No prose, no sub-headings, no bullets there.
- Titles/details: terse, append-only — never reword the user's original text.
==============================================================================
Carried over from .prompts/2026-06-06.md: Task 6 (the only unfinished task).
Task number preserved so plan-doc / branch references stay valid.
==============================================================================
-->

## Task 6: datagen full modularization

Stage 2 of the datagen refactor (decision A, Task 5). Split the monolith into
`RAGE::Datagen::*` modules, one at a time.

- Split the monolith (`tools/datagen.pl`, ~5148 lines) into `RAGE::Datagen::*`
  modules: the 1229-line `read_input_data` parser, the ~1500 lines of
  `generate_*` emitters, flow-rules, datasets/codesets, tracker.
- **Prerequisite:** introduce a **context object** to replace the ~30 file-scoped
  globals (e.g. `@h_game_data_lines` — 87 refs; the `@all_*` model arrays + their
  `*_name_to_index` hashes; the emit accumulators). Without it, extracted modules
  just reach back into `main::` and nothing is actually decoupled.
- **Method:** one module at a time, **each extraction gated on `make
  all-test-builds` + `tests/00regression/` green and ZX byte-identical**. NOT a
  big-bang rewrite (avoid large untested modifications).
- Modules under `lib/RAGE/` (RAGE1 convention), per Task 5 decision #1.
- Work in the "datagen_full_modularization" branch.
- Use ultrathink.

### Plan + progress (carried from 2026-06-06)

- Branch: **datagen_full_modularization** (off refactor_for_multiplatform). NOT pushed.
- Context-object design: a `RAGE::Datagen::Context` blessed hashref holding the
  model arrays + name→index hashes + accumulators, introduced via a TEMPORARY
  glob-aliasing scaffold (`*main::all_btiles = $ctx->{all_btiles}`) so each sub
  extraction is mechanical, byte-preserving and reversible.
- **STEP STATUS (live — kept current as each step is worked; [x]=committed,
  [~]=in progress, [ ]=not started). Leaf-first, PARSER LAST. Each step gated
  byte-identical (ZX+CPC) + independent review before commit:**
  - [x] 0 Context+scaffold — commit 8e6bfa0
  - [x] 1 ColorTokens — commit 2ba79ce
  - [x] 2 Util — commit b82500d
  - [x] 3 PngDispatch — commit 8e6bfa0
  - [x] 4 BuildFeatures — commit c811370
  - [x] 5 Validate — commit 7bcb29f
  - [x] 6 Sprites — commit f4ceec8 (byte-gate 0-diff; review APPROVE; all-test-builds rc=0 18 games)
  - [x] 7 Btiles — commit 9261ceb (byte-gate 0-diff 11 games incl CPC bpc=16+mono; review APPROVE; all-test-builds rc=0 18 games)
  - [x] 8 Screens — commit 3179c7e (byte-gate 0-diff 11 games; review APPROVE 3 cosmetic nits; all-test-builds rc=0 18 games)
  - [x] 9 FlowRules — commit ff1b747 (byte-gate 0-diff 11 games incl dedup-heavy; review APPROVE; all-test-builds 18/18 on CLEAN rerun — 1st run spuriously failed due to concurrent reviewer git-stash clobbering build/)
  - [x] 10 Hero/Entities — commit 4206ce6 (module RAGE::Datagen::Entities; byte-gate 0-diff 11 games; review APPROVE 0 nits, read-only reviewer ran concurrent w/ build cleanly; all-test-builds 18/18)
  - [x] 11 Codesets/Tracker/GameConfig — split into two modules, both byte-gate 0-diff + review APPROVE + all-test-builds 18/18:
    - 11a GameConfig — commit 2e0ad8a (RAGE::Datagen::GameConfig: game_functions/areas/h_header/ending/game_config/misc_data/conditional_build_features/configuration_values; +single_game_area internal)
    - 11b Codesets — commit d42707b (RAGE::Datagen::Codesets: c_home/banked headers, global_codeset_data, codeset_headers/functions, custom_function_tables, binary_data_items, tracker_data; +all_codesets_except_home internal)
  - [x] 12 Dependencies — commit f259329 (RAGE::Datagen::Dependencies: create_dataset_dependencies, fix_feature_dependencies, derive_cpc_audio_backend_features; 0 new aliases needed; byte-gate 0-diff; review APPROVE 0 nits; all-test-builds 18/18)
  - [x] 13 Parser (read_input_data) — commit 136bb7b (RAGE::Datagen::Parser; ~1216-line state machine; scaffold += item_name_to_index, crumb_type_name_to_index, valid_trackers, forced_build_target; byte-gate 0-diff; review APPROVE body byte-for-byte identical; all-test-builds 18/18; datagen.pl 2073->862 lines)
- **Byte-identity gate** = the `.asm` + `features.h` + DETERMINISTIC `.c`
  (`PERL_HASH_SEED=0 PERL_PERTURB_KEYS=0`): plain `game_data.c` differs run-to-run
  via `btile_deduplicate_arena_best` hash-ordering (PRE-EXISTING non-determinism,
  proven by 2 unchanged-code runs diffing). Fast datagen-only gate: `make config
  data target_game=games/X` (then diff build/generated/{game_data.c,asm_game_data.asm,
  features.h} vs a baseline). Full functional gate: `make all-test-builds` (18 games).
- **Per-step notes (modules under lib/RAGE/Datagen/):**
  - Step 0+3 (8e6bfa0): `Context` (context obj + `install_main_aliases` glob-alias
    scaffold) + `PngDispatch` (dispatch_png_asset_handling + _cpc_mono_dispatch).
    KEY MECHANISM: datagen globals are `my` lexicals; `*main::x = \@lexical` aliases
    via a REF to the lexical — valid, tracks reassignment. Added
    tests/datagen/png_dispatch.t (28 subtests) for the CPC-mono path (no test game
    hits it). 2 review NITs fixed (comment-only).
  - Step 1 (2ba79ce): `ColorTokens`. Step 2 (b82500d): `Util` (optional_hex_decode/
    pixels_to_byte/integer_in_range; integer_in_range is dead code, moved per plan).
  - Step 4 (c811370): `BuildFeatures` (add_build_feature/is_build_feature_enabled/
    add_default_build_features/input_backend_for_platform/get_gfx_backend +
    @default_build_features). Scaffold += conditional_build_features (shared mutable
    hash). asset_backend stays in datagen.pl.
  - Step 5 (7bcb29f): `Validate` (check_game_config_is_valid + run_consistency_checks
    exported; 3 screen-checks internal). Imports sibling helpers. Scaffold +=
    all_btiles/all_screens/all_sprites/all_items.
  - Step 6 (f4ceec8): `Sprites` (validate_and_compile_sprite + generate_sprites
    exported; generate_sprite internal). Scaffold += c_dataset_lines (emit
    accumulator) + dataset_dependency. First module→datagen callback: main::asset_backend().
  - Step 7 (9261ceb): `Btiles` (validate_and_compile_btile + generate_btiles
    exported). Scaffold += h_game_data_lines (header emit accumulator) +
    btile_name_to_index. Callbacks: main::asset_backend() (shared w/ Sprites) +
    main::btile_deduplicate_arena_best() (lives in main:: via RAGE::BTileUtils,
    which has no package decl). conditional_build_features alias already present.
  - Step 11 (2e0ad8a + d42707b): split into `GameConfig` (11a) + `Codesets`
    (11b). generate_game_data/output_game_data stay in datagen.pl as the driver
    (call the imported emitters). 11a scaffold += cfg, build_dir,
    h_build_features_lines, valid_game_functions (decl moved up like $syntax).
    11b scaffold += 10 codeset/dataset/tracker accumulators+addr globals;
    callbacks main::file_to_bytes/file_to_compressed_bytes (RAGE::FileUtils) +
    main::arkos2_* (RAGE::Arkos2) — both package-less libs → subs live in main::.
    PROCESS: large interleaved clusters delegated to impl agents (own context),
    self-verified via datagen-only byte-gate; I re-verified gate + read-only
    review + full build before commit. Watch for duplicate Context aliases when
    an agent re-aliases a global an earlier step already added (11b nit fixed).
  - Step 10 (4206ce6): `Entities` (validate_and_compile_hero, generate_hero,
    generate_bullets, generate_items, generate_crumb_types — all exported).
    Scaffold += hero (SCALAR alias) + c_game_data_lines (main C emit
    accumulator, shared w/ staying output emitters). Imports add_build_feature.
    GOTCHA: the naive `^sub..^}` brace-matcher mis-finds sub ends when a heredoc
    body contains a line starting with `}` (e.g. `};` in EOF_ITEMS2/EOF_CRUMBS2)
    — use real boundaries from reading, not the matcher, when heredocs emit `}`.
  - Step 9 (ff1b747): `FlowRules` (find_existing_rule_index,
    validate_and_compile_rule, generate_flow_rules,
    generate_game_events_rule_table exported; generate_rule_checks +
    generate_rule_actions internal). The $check_data_output_format /
    $action_data_output_format tables moved into the module as private lexicals
    (only used by the emitters). Scaffold += all_rules, max_flow_var_id (SCALAR
    alias — shared w/ the GAME_NUM_FLOW_VARS feature emitter that stays),
    check/action_custom_function_id + check/action_custom_functions,
    game_events_rule_table. Imports add_build_feature + Data::Compare (Compare =
    rule dedup); the now-unused `use Data::Compare` was dropped from datagen.pl.
    PROCESS NOTE: run the review agent BEFORE all-test-builds (or in a worktree)
    — a concurrent reviewer git-stash+make clobbered build/ and failed the 1st
    build spuriously; clean rerun = 18/18. See [[feedback_no_concurrent_reviewer_builds]].
  - Step 8 (3179c7e): `Screens` (validate_screen, compile_screen,
    generate_screens, generate_map, generate_global_screen_data exported;
    compile_screen_data + generate_screen internal). Scaffold +=
    sprite_name_to_index, screen_name_to_index, all_crumb_types, syntax. The
    `my $syntax` decl was moved up ahead of the scaffold block (lexical scope)
    so it can be aliased; literal unchanged. Imports add_build_feature +
    is_build_feature_enabled from BuildFeatures. PATTERN NOTE: any config/global
    a future module needs that is declared AFTER the scaffold block must first
    be hoisted above it (scope rule), like $syntax here.
- SCAFFOLD GROWTH: the context object seeds minimal and grows one entry per step
  (only the globals that step's module touches). Aliases installed at file-load
  before any runtime call.
- NOTE: prompt-file divergence — 2026-06-06.md (branch) carries committed Step 1-3
  notes from before this file existed on the branch; THIS 2026-06-07.md is the
  authoritative Task 6 tracker going forward. Resolve any 2026-06-06.md merge
  conflict toward the trunk copy when the branch lands.

## Task 7: parallelize test game builds

- Use ultrathink
- A full regression test suite compiling all games takes a long time because each game needs the whole "build" directory for itself so several builds cannot be launched in parallel
- Design a plan for making the build targets use a different build directory per game to allow parallelization
- Make the all-test* targets use the parallel implementation, up to a  maximum nhumber of default builds, that can be specified in the command line (e.g. MAX_PARALLEL_JOBS=4). Default 4 simultaneous builds
- This allows to keep the game artifacts intact after building each game, which allows testing several of them visually in a row
- Do it for bozt ZX and CPC targets
- Consider the possibility of easily adding new test games (specially for CPC)
- Plan first, then allow me to review the plan
- Implement after my validation
- Use a new branch "parallel_regression_tests" to work on this

## Task completion status

- [x] Task 6: datagen full modularization (Stage 2) — FINISHED, user-confirmed 2026-06-07.
  Steps 0-13 split datagen.pl into 13 RAGE::Datagen::* modules (5148->862 lines, all
  byte-identical + 18/18 builds). PLUS follow-up DONE: temporary glob-alias scaffold
  REMOVED via explicit $ctx threading — Phase 1 ec60742 ($ctx = canonical store),
  Phase 2 34d90c9 (thread $ctx through all modules, delete main:: bridge). Plus 6b69bbb
  (write generated files via :encoding(UTF-8) — silence Wide-character warnings, byte-
  identical). Zero residual main:: field refs; byte-identical; 18/18; reviews APPROVE.
- [ ] Task 7: parallelize test game builds (plan-first → review → implement on branch parallel_regression_tests)
