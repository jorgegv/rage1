# TASKS

## Task 1: continue with JSP integration

- Evaluate the GENERIC SPRITE LIB REFACTOR document for context
- Evaluate the ADDING JSP SPRITES document for the plan
- Continue work with the plan

## Task 2: create a skill for automated testing

- help me create a skill for automated testing using JNext, my own ZX emulator
- just use it. the executable lives in either ~/src/spectrum/jnext/build/jnext or ~/src/spectrum/jnext/build/gui-release/jnext (depending on the bild conditions; you can use any of them for tests).
- Run it with --help to get the CLI options, they are explained there. If you have any doubts we can discuss

## Task 3: debug minimal_jsp

- minimal_jsp is not working, we need to debug


## Task Completion Status:

- [x] Task 1: JSP Phase 4 — partial
  - [x] P4-5: `make all-test-builds` — 12/12 games pass, no regressions on SP1 path
  - [x] P4-6: `make build target_game=games/minimal_jsp` + `make mem` — JSP 48K layout verified, no address overlap, 15261 bytes free
  - [x] P4-1/P4-2/P4-3/P4-4/P4-7/P4-8 — pending (blocked on Task 3 / JSP rendering fix)
- [x] Task 2: JNEXT regression skill — complete
  - Framework at `tests/00regression/` (regression.sh, README.md, .gitignore)
  - Per-test config schema (`TARGET_GAME`, `MACHINE`, `DELAY_FRAMES`, `EXTRA_ARGS`)
  - Pixel-perfect diff vs checked-in baseline via ImageMagick `compare -metric AE`
  - Skill at `.claude/skills/jnext-regression/SKILL.md`
  - Seeded with `minimal` test (48K, 150 frames, PASS at 0 px diff)
- [x] Task 3: debug minimal_jsp — pending
  - JSP visual differs from SP1 for same game: hero sprite not rendered, stale asmloader progress markers remain on screen, partial game-area border. Confirmed not a frame-delay issue (stable across 300/500 frames).
  - Cannot seed `minimal_jsp` regression test until the visual matches `minimal` (otherwise we'd lock in a broken baseline).
  - User noted JSP adaptation is in progress — defer to next session.
