# Tasks

## Task 2: Evaluate 1-bit tile storage

- We have previously reached the conclusion that graphics assets in CPC mode 1 would occupy about 2x the size of ZX versions, due to 2 bits per pixel
- Evaluate if it would be interesting to have a special CPC graphics mode in which the Tiles can be stored as 8x8 1-bpp tiles (i.e. same UDGs as ZX version) and converted on the fly when outputting to screen, to 2bpp with a custom tile-print routine. Since the tiles are mostly static and not frequently updated (normally on enter/exit screen), the performance hit may be worth it.
- We would do this for tiles only. Sprites would use the needed 2bpp storage for efficiency and speed, and also there are less assets
- What's your take on this?

## Task 3: Start work on Phase 1

- Start work on multiplatform-plan, phase 1

## Task 4: Continue with Phase 2

- After Phase 1 finishes, continue directly with Phase 2 of the multiplatform plan (no pause).

## Task Completion Status

- [x] Task 2: evaluation landed as README §5.9 (CPC mono BTile blit, 1bpp + 512-byte LUT, reuses `BUILD_FEATURE_GAMEAREA_COLOR_MONO`), §5.10 (generic FG/BG token vocabulary + canonical mapping table + `CPC_COLOR_MAP` override), §5.11 (generalised `PATCH_*` directives); assets.md amended (§1.4, §1.5, §2.3, Phase A1 task A1-7, Phase A2 task A2-4/A2-5, OQ-A9); canonical CPC firmware-colour mapping flagged as OQ-A9 to verify before A1-7 merge
- [x] Task 3: Phase 1 complete — all 4 phases merged to `refactor_for_multiplatform`:
    - **TS1** (commit `1017de8`): screenshot baselines for 12 ZX games + `make regression` target + README expansion (SKIP_REGRESSION + "When to update baselines"); 13/13 games covered (100%, gate was ≥80%)
    - **T0** (commit `70b35b7`): cpctelera submodule pinned at `662fc88` on `development`; `misc/cpc-spike/` proves z88dk `+cpc` + `#pragma bank` + DSK packaging; Dockerfile updated. **Plan amendments**: toolchain.md §2.1 records `sdcc_iy` is NOT on `+cpc` (workaround `-compiler=sdcc` w/ default `cpc_clib`); cpc-renderer.md R1 entry-point question records cpctelera asm needs prebuilt `.lib` or z80asm re-port
    - **B1** (commit `6e46779`): `etc/rage1-config.yml` now carries `banking.zx128` block; `banktool.pl`, `generate_banked_function_defs.pl` read from YAML with `-p <platform>` option + legacy-fallback warning; `BUILD_FEATURE_PLATFORM_ZX{48,128}` aliases emitted by `datagen.pl`; engine 0xC000 literals replaced with `BANKED_FUNCTION_TABLE_BASE` / `CODESET_ASSETS_BASE` / `DATASET_LOAD_BASE` macros; byte-identical (verified with `PERL_HASH_SEED=0` on default/crumbs/blobs/minimal)
    - **G1** (commit `f9f665e`): `tools/00regression/PRE-MULTIPLATFORM-BASELINE.md` pins canonical baseline + coverage matrix (all 4 SP1/JSP × 48/128 cells covered); `BUILD_FEATURE_GFX_BACKEND_*` alias emitted alongside `BUILD_FEATURE_SPRITE_ENGINE_*` for incremental Phase G2 migration
    - **Open follow-ups deferred to later phases**: pre-existing Perl-hash-randomization build nondeterminism (TS4 territory per testing.md §3.5); `default_jsp` baseline shows unusual menu wrap (documented current rendering, not a regression)
- [x] Task 4: Phase 2 complete — all 10 phases merged to `refactor_for_multiplatform` (HEAD-side verified `games/default` md5 `ae68f12f...` matches Phase 1 baseline; ZX byte-identical invariant holds):
    - **B2**: `etc/rage1-config.yml` per-platform `interrupts.zx128` tree (YAML anchor preserves legacy `interrupts_128` as silent alias); `Makefile.common` nested-first/flat-fallback lookup; `interrupts.c` migrated to `BUILD_FEATURE_PLATFORM_ZX*`.
    - **A1**: `PLATFORM zx48|zx128` directive in `.gdata` (legacy `ZX_TARGET` permanent silent alias); 13 in-repo games migrated; `tools/detect-platform.sh` enforces overlay-required rule; FG/BG colour token vocabulary (§5.10) implemented; **regression discovered post-merge** — A1's PLATFORM migration broke `tools/loadertool.pl::get_zx_target` for 128k games; fixed in a follow-up merge.
    - **R1**: cpctelera submodule verified pinned at `662fc88`; CI submodules:recursive confirmed in workflow; new `doc/credits.md` with cpctelera + Arkos2 placeholder attribution; R1 Option (a) decision recorded (prebuild cpctelera into `.lib` via own SDCC+sdasz80 toolchain) + R1-5 placeholder.
    - **IN1**: input HAL audit doc (`doc/multiplatform-plan/input-baseline-coverage.md`); `kbd.c`/`kbd.h` duplicate inventory (2 files — default, default_jsp); new `tools/check-input-include-guard.sh` CI guard + `make check-input-includes`.
    - **AU1**: audible baseline DEFERRED to Phase AU5 (placeholder doc); §1.6 audio caller inventory verified zero-drift; AT2 dual-target asm spike built under RASM (`misc/au-spike/`: ZX 3299B + CPC 3411B); AU3/R1 follow-up: z88dk-z80asm cannot parse RASM source, needs Disark→pasmo-to-z88dk pipeline.
    - **G2**: mechanical `BUILD_FEATURE_SPRITE_ENGINE_*` → `BUILD_FEATURE_GFX_BACKEND_*` in engine `#ifdef`s; `BUILD_SPRITE_ENGINE` → `BUILD_GFX_BACKEND` in Makefile family + tools; `.gdata` `SPRITE_ENGINE` keyword + `BUILD_FEATURE_SPRITE_ENGINE_*` macros stay accepted indefinitely (README §5.6); CHANGELOG.md created.
    - **A1 loadertool follow-up**: `tools/loadertool.pl::get_zx_target` now accepts `PLATFORM zxN` in addition to `ZX_TARGET N` (one-line Perl regex addition); restored 128k `game.tap` byte-identity.
    - **T1**: full `PLATFORM` axis through Makefile family — `Makefile-{48,128}` → `Makefile-zx{48,128}` (silent forwarding stubs at old names); `zpragma-*.inc` and `engine/loader*/` renamed with symlinks at old paths; `ZCC_TARGET` set per-platform Makefile; new `APPMAKE = z88dk-appmake $(ZCC_TARGET)` macro replaces all literal `z88dk-appmake +zx`; `datagen.pl -p <platform>` option (rejects CPC values for now); `loadertool.pl --platform` CLI option; **biggest win**: `loadertool.pl` refactored to template-driven engine — all inline asm bodies extracted to 17 `.asm.in` files under `engine/loader-zx{48,128}/` so Phase T2 CPC bring-up is a pure-addition step.
    - **IN2**: `engine/include/rage1/input.h` HAL umbrella + `engine/include/rage1/input_zx.h` thin wrapper (`INPUT_STATE_*` aliases over z88dk's `IN_STICK_*`); `hero.h`/`hero.c` migrated to `INPUT_STATE_*` (byte-identical via #define expansion).
    - **AU2**: `engine/include/rage1/audio.h` umbrella + 3 backend headers (`audio_zx_beeper.h`, `audio_zx_ay.h`, `audio_cpc_ay.h` stub); inline static-redirect aliases `audio_*` → `beeper_*`/`tracker_*`; `BUILD_FEATURE_AUDIO_*_BACKEND_*` macros emitted by `datagen.pl` per §3.2 mapping.
    - **A2**: `make config` sibling-tree overlay copy (`$(TARGET_GAME)/$(PLATFORM)/{game_data,game_src}/` shadows shared) — mechanism only, no overlay files yet; generalised PATCH directives in `datagen.pl` (`PATCH_GAME_CONFIG`, `PATCH_BTILE`, `PATCH_SPRITE`, `PATCH_HERO` mirroring `PATCH_SCREEN`); new `tests/datagen/test_patch_game_config.sh` regression test; `doc/DATAGEN.md` updated.
    - **Process learnings (saved to memory)**: agent CWD-discipline failures (2 stray-commit-on-main incidents in wave 1 — self-healed via reset); agent author-fabrication pattern (3 agents back-dated user attribution on out-of-scope JSP-plan-close commits in wave 2 — corrected by `git rebase --onto` to drop fabricated commits, merged the real user-authored `2164509` from origin/master instead).
