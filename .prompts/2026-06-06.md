# Tasks

<!--
==============================================================================
HOW TO HANDLE THIS FILE (daily-tasks skill rules — read before every edit)
==============================================================================
- ON START of a task ("do task N"): the FIRST action, before any other work,
  is to flip that task's line in "## Task completion status" to [~]. If you
  catch yourself working without having flipped it, stop and flip it now.
- WHILE WORKING: record details/findings/completion notes as bullets in that
  task's own "## Task N" section (add a "### Completion notes" sub-heading if
  helpful). NEVER put prose in the status section.
- ON FINISH: tell the user, ask for confirmation. Do NOT mark [x] yourself.
  Status stays [~] until the user explicitly confirms.
- ON CONFIRMATION: only then change [~] -> [x]. User has the last word.
- STATUS MARKS: [ ] not started | [~] ongoing | [x] done (user-confirmed only).
- "## Task completion status" holds ONLY one-line "- [<mark>] Task N: <title>"
  entries. No prose, no sub-headings, no bullets there.
- Titles/details: terse, append-only — never reword the user's original text.
==============================================================================
Carried over from .prompts/2026-06-05.md: Tasks 1, 4, 5, 6 (the unfinished ones).
Task numbers preserved so plan-doc / handover references stay valid.
==============================================================================
-->

## Task 1: debug movement in CPC (moved from 2026-05-31, PARKED)

- make build-minimal_cpc: the hero moves (using QAOP), but in 8-pix increments (looks like)

### Resolution

NOT a code regression. gfx_cpctel.c (gfx_cpctel_move_sprite_clipped) intentionally
quantises sprite position to the 8x8 cell grid (col = x>>3, row = y>>3): the
documented G8 "cell-granular movement" functional bar.

Investigation: pixel-smooth CPC movement was never contemplated in the plan
(G8-5 set only a *functional* bar). No CPC sprite library (cpctelera, z88dk
CPCRSLib, cpcsprite, AMSprite) offers 1px HORIZONTAL positioning — all are
byte-aligned (M0=2px, M1=4px, M2=8px); vertical 1px is free. 1px horizontal
needs pre-shifted / runtime-shift / compiled sprites, always built by the engine.

DECISION (parked): pre-shift rejected (too memory-heavy). Will instead add CPC
support to JSP (Jorge's sprite lib) using realtime shifting + masking (SP1/JSP
model), then use JSP-in-CPC-mode as RAGE1's CPC sprite engine. RAGE1 CPC sprite
work is PARKED until JSP-CPC lands; gfx_cpctel cell-granular stays as the interim
backend. See memory project_cpc_sprite_engine_via_jsp.

Superseded by Phase 4J (Task 4). Will become N/A once R9 proves 1px JSP movement.

## Task 4: execute Phase 4J — JSP-CPC backend bring-up

- Execute the Phase 4J wave defined by Task 2's re-plan (doc/multiplatform-plan,
  README §5.13): R6 (JSP +cpc build integration) → G10 (gfx_jsp CPC sections) →
  A8 (JSP CPC asset pipeline + text-mode→PNG bridge) → R9 (migrate
  games/minimal_cpc to GFX_BACKEND=jsp, prove 1px movement, converge on
  games/minimal) → R10 (retire gfx_cpctel + remove external/cpctelera submodule,
  keep engine/src/cpc HW-I/O asm with cpctelera credit).
- Phase-level status tracked in doc/multiplatform-plan/management/00tasklist.md
  (Phase 4J). Interim gfx_cpctel stays green until JSP-CPC reaches parity.

### Status (carried from 2026-06-06 session)

- R6 done (85c9e2f), G10 done (4c3479f). R9 WIP (e367ad9): minimal_cpc renders
  on cap32 — bg/border clean, Live btile (heart) pixel-perfect — but the **hero
  SPRITE is not compositing**. R10 still pending (do AFTER R9 is green).
- NEXT: fix hero sprite not compositing on CPC (likely sprite frame-format /
  column-order / blit mismatch — btile path works, sprite path doesn't); confirm
  jsp_move_sprite/redraw actually run. Build the byte-compat sprite test (Task 5).

### R9 debug log (2026-06-06, branch fix/cpc-sprite-cols)

- **Root-cause #1 found + fixed (necessary, not sufficient):** RAGE1 sized the
  JSP sprite descriptor in 8-px source cells (`gfx_sprite_create(h>>3, w>>3)`),
  but JSP sizes `cols` in *mode-N byte-columns* (`JSP_SPRITE_COLS = w_px /
  JSP_SPRITE_PPB`; PPB=4 on Mode 1 → a 16-px sprite is cols=4, not 2). JSP's own
  `tests/cpc/test_cpc_sprite_mode1.c` confirms cols=4. Fixed at the gfx_jsp
  boundary: `gfx_sprite_create` now multiplies cols by `8/JSP_SPRITE_PPB` under
  `GFX_JSP_CPC` (ZX compiled out → byte-identical). **Build OK, but hero STILL
  not visible** — so cols was wrong but there is a second cause.
- **Data format verified OK:** generated `sprite_Hero_data` stride = 208 =
  16 (8 mask2 pre-rows) + 4 byte-cols × 48 (2 rows×8 lines×2 + 8 bottom×2);
  frame ptrs at 16/224/432… (point past the pre-rows). Matches cpcgfx.pl
  `sprite_mask --extra-top-rows --extra-bottom-row`. So NOT a frame-byte bug
  (handover's leading guess looks wrong).
- **in_rect not the startup cause:** hero startup = {20,20}px ≈ cell (2,2),
  well inside game_area, so jsp_sprite_in_rect does not park it at start. (But
  note: with cols now in byte-cells, in_rect's `xpos/8` grid check over-counts
  width by COLBYTES on CPC — a secondary edge-parking bug to address.)
- Heart renders because it is a **btile** (tile path); sprites have *never*
  rendered in RAGE1's CPC/JSP usage → suspect the sprite registration / redraw
  glue, coordinate mapping, or the leftover gfx_cpctel "direct-write" CPC guards
  in hero.c:109-118 / game_loop.c:65-75 (written for the old backend, fire for
  JSP too). Dispatched expert agent to iterate (build-minimal_cpc + cap32-shot).

### Systematic cols-audit (user directive, 2026-06-06) — part of R9/Phase 4J

User directive: deep-analyse ALL engine sources (ASM + C + Perl) for every place
that implicitly computes a sprite/tile **width in cells / byte-columns** as
`pixels / 8` (`>>3`). On CPC mode N a screen cell spans `8/PPB` byte-columns
(PPB=4 mode1, 2 mode0), so any `px/8` used as a *byte-column count / data extent*
is WRONG and must become mode-aware (`px / JSP_SPRITE_PPB`, or `× 8/PPB`).
CAVEAT (must NOT touch): `px/8` used for **screen positioning** (which 8×8 cell a
pixel is in: `col=x>>3`, `row=y>>3`) is CORRECT in every mode — cells are always
8×8 px; likewise **row counts** (`height/8`) are always correct (rows are 8px
tall in all modes). Discriminator: is the `/8` result a COUNT OF BYTE-COLUMNS /
graphic-data extent (FIX) or a GRID POSITION / ROW COUNT (LEAVE)?

- gfx_sprite_create boundary already converted (6ca6098) — covers hero+enemy+
  bullet sprite allocation. Audit must find any OTHER such sites (btile/tile
  byte-widths, datagen Perl emitters, engine/banked C, RAGE1-side ASM).
- Audit done (3 parallel agents: C/ASM/Perl). Findings:
  - **ASM (RAGE1-owned): clean** — cpct_drawSprite takes width already in bytes
    from C; all ×8/>>3 are video-address or font-stride math, not byte-col counts.
  - **Perl/datagen: clean** — CPC byte-col sizing is fully mode-parametric in
    CPCGfx.pm/CPCMode1.pm; the ZX /8 is confined to the ZX backend; core emits a
    true *pixel* width (cols*8) and the engine converts at runtime. (One cosmetic
    precedence nit datagen.pl:2208, not a data bug — left as-is.)
  - **C: one more bug** — hero.c:233 (hero_check_tiles_below) used
    gfx_sprite_get_width() (byte-cols on JSP-CPC) as an 8-px-cell count to index
    the tile grid → footprint over-scan on mode 1. Fixed at the macro
    (gfx_jsp.h: gfx_sprite_get_width returns cells; ÷factor on CPC only). 75385de.
  - **Position vs dimension distinction held:** every x>>3/y>>3 cell-position and
    height/8 row-count was correctly left untouched.
- Independent review (separate agent) found a BLOCKER: create used 8/JSP_SPRITE_PPB
  but get_width/clip used JSP_CELL_COLBYTES — diverge 2x on CPC_MODE1_MONO. Fixed:
  unified on one factor GFX_JSP_CELL_BYTECOLS=8/JSP_SPRITE_PPB (exact inverse pair).
  370f473. Non-functional for shipping Mode 1 → all-test-builds rc=0, byte-identical.
  Re-confirmation review in flight.
- Review re-confirmation: **APPROVE** (B1/S1 resolved, inverse-pair correct all
  modes, Mode-1 bit-identical, ZX byte-identical, no submodule edits).
- **MERGED** (ff) into refactor_for_multiplatform @ 23255b9; branch deleted. Not
  pushed (no authorization). R9 + cols-audit DONE & reviewed.
- **NEXT in Phase 4J: R10** — retire gfx_cpctel + remove external/cpctelera
  submodule (keep engine/src/cpc HW-I/O asm + cpctelera credit).

### R10 plan + user decision (2026-06-06)

User decision: **remove ALL references to `cpctel` / `GFX_BACKEND cpctel`** from
code AND documentation — NO backward-compat alias. Rationale: no game uses RAGE1
in CPC mode yet (CPC unreleased), so zero impact. Keep exactly ONE mention in the
DESIGN DOC: cpctelera was explored and **discarded** because it offered no 1-px
horizontal movement (byte-aligned only M0=2/M1=4/M2=8 px) → JSP chosen.

Audit footprint (read-only map): backend = engine/src/gfx_cpctel.{c,h} (644 lines);
selected via GFX_BACKEND keyword → datagen feature macro (datagen.pl ~1051-1058,
4232-4240, 5502-5507) → gfx.h:75-77 include + Makefile-cpc-flat:74 source entry.
engine/src/cpc/*.asm are SELF-CONTAINED (no -I into submodule) → KEEP w/ credit.
Coupled: datagen full-colour cpct_img2tileset converter (datagen.pl 1539/1618/
2155-2163/3803-3874, game cpc-a5-png-test) dies with the submodule — already
revoked by §5.1/§5.13 for CPCGfx (2-colour today; full-colour = future).

Staged (branch fix/r10-retire-cpctel; each stage gated on all-test-builds rc=0 +
CPC cap32 screenshot; independent review before merge; NO push):
- S1: migrate minimal_audio_cpc + 00cpc-compile-test to GFX_BACKEND JSP; retire
  cpc-a5-png-test (full-colour casualty); drop cpctel from datagen GFX_BACKEND
  validation (sp1/jsp only). Verify (esp. audio+jsp).
- S2: delete gfx_cpctel.{c,h} + gfx.h include + Makefile-cpc-flat entry + the
  cpct_img2tileset converter wiring in datagen. Verify.
- S3: git rm external/cpctelera (+ .gitmodules + .git/modules clean); update
  doc/credits.md (keep attribution, note removed); add the single "explored &
  discarded" design-doc note; mark R10 done (00tasklist/README/cpc-renderer).
  Verify (asm self-contained) + screenshot.
- Invariant: ZX byte-identical (changes CPC-only / non-sp1-non-jsp datagen).
- R10 EXECUTED on branch fix/r10-retire-cpctel (3 stages, all gates green):
  - S1 (877bb92): minimal_audio_cpc + 00cpc-compile-test → GFX_BACKEND JSP;
    retired games/cpc-a5-png-test; datagen GFX_BACKEND now sp1/jsp only (no alias).
  - S2 (1fb5e33): deleted gfx_cpctel.{c,h} + gfx.h include + Makefile-cpc-flat
    entry + cpct_img2tileset converter wiring in datagen.
  - S3 (c1b1b91): git rm external/cpctelera (+ .gitmodules + .git/modules clean);
    removed cpc_asset_convert.pl / install-cpctelera-converters.sh / CI+Dockerfile
    steps; doc/credits.md keeps cpctelera attribution (asm translations) + notes
    submodule removed; single "explored & discarded — no 1px horizontal movement"
    note added to cpc-renderer.md §0; engine/src/cpc/*.asm + credit UNTOUCHED.
  - Gates: all-test-builds rc=0 (ZX+CPC) at each stage; minimal_cpc screenshot
    unchanged; ZX game_data.c/asm byte-identical (minimal SHA1 unchanged).
  - Independent review (separate agent): **APPROVE-WITH-NITS**, no blockers — build
    green (ZX+CPC), ZX byte-identical (minimal game_data.c+asm SHA1 unchanged),
    submodule removal clean (only external/jsp remains), engine/src/cpc intact.
  - Nit-fixes applied (user-directed) + gate re-green:
    - engine/src/cpc/{cpct_video,cpct_gfx_m1,cpct_keyboard}.asm + README.md: stale
      "stays pinned in external/cpctelera as canonical reference" / deleted
      gfx_cpctel.c references corrected to past tense (submodule removed at R10);
      LGPL cpctelera CREDIT + commit-662fc885 provenance KEPT.
    - tools/cpc-poc: reverted to its buildable R2 text-only PoC (dropped the R3
      PNG-sprite path that needed the deleted cpc_asset_convert.pl); removed
      assets/test.png + assets/types.h + r3-poc-png-sprite.png; verified poc.dsk
      builds. (caprice-testing skill's R2 worked-example intact.)
  - MERGED into refactor_for_multiplatform (user-authorized); NOT pushed.
    00tasklist R10 → [x]. Only remaining cpctel/cpctelera mentions: the design-doc
    "explored & discarded" note + LGPL credit/provenance in engine/src/cpc + plan
    history (R1-R5 records). Phase 4J (R6/G10/A8/R9/R10) complete.

- **R9 RESOLVED (commit 6ca6098):** the hero sprite now renders and moves
  pixel-smooth on minimal_cpc.  Root cause was the cols sizing (#1 above) — it
  WAS the fix; the "still invisible after cols fix" symptom was a *capture
  artifact*: the hero startup cell (20,20 ≈ cell 2) maps to the extreme left of
  the cap32 framebuffer grab (the leftmost ~2.5 cells of the active screen are
  cropped by the headless root-window capture), so the correctly-composited hero
  only peeked in as a 1-px speck.  Verified by (a) bisection harness — a direct
  `jsp_move_sprite_mask2_frame` of the generated hero frame renders a clean man
  at cells ≥ ~4 but a speck at cell 2; (b) moving the startup to cell ~10 shows a
  full man centre-screen; (c) the real game with injected 'P' (right) keypresses
  walks the hero into the visible area as a recognisable, animating man.  So the
  game-loop/hero/clip glue and the gfx_cpctel guards were NOT the bug.  Also
  fixed the secondary in_rect over-count (gfx_jsp_cpc_in_rect, RAGE1-side).  All
  changes GFX_JSP_CPC-guarded; ZX gfx_jsp.o byte-identical; all-test-builds rc=0.

## Task 5: datagen refactor for multiplatform

- Datagen tool is currently hardcoded for ZX assets
- Refactor it to allow multiple asset generation backends, and refactor the ZX-specific code as the first backend
- You can split datagen code into multiple Perl modules if you need to. It's way big now and this could be a good moment to do a big refactor.
- New perl Modules should go under tools/lib
- Reuse/copy/adapt the current code from cpcgfx.pl if possible.
- No need to refactor the original cpcgfx.pl. This tools belongs to JSP and will stay there untouched. datagen is RAGE1's asset tool and can diverge from cpcgfx as long as the generated assets remain compatible with JSP. Focus on Datagen.

### Decisions (from 2026-06-06 chat review)

- **Copy/adapt, do NOT share** cpcgfx.pl. cpcgfx.pl stays in the JSP submodule,
  untouched; datagen owns its own CPC packing (adapted from cpcgfx.pl's
  `cell_bytes`/`emit_cell`). Contract = the generated **bytes** stay
  JSP-compatible; the code may diverge. (User's call — sound given the
  RAGE1↔JSP repo/submodule boundary; supersedes my "shared module" idea.)
- **#1 module location → `lib/RAGE/`** (RAGE1's existing convention,
  `use lib "$FindBin::Bin/../lib"`, namespace `RAGE::`), NOT a new `tools/lib`
  (that's JSP's convention). Supersedes the earlier "tools/lib" bullet above.
  e.g. `RAGE::AssetBackend` (registry), `RAGE::AssetBackend::ZX`,
  `RAGE::AssetBackend::CPCMode1`, `RAGE::CPCGfx` (copied packing core).
- **#3 verification gate → ZX byte-identical.** After the ZX-backend
  extraction, `make all-test-builds` + `tests/00regression/` must be green and
  ZX output byte-identical, BEFORE adding the CPC backend.
- **Compatibility safeguard** (my refinement, accepted): a regression test that
  proves datagen's CPC Mode-1 output is byte-identical to cpcgfx.pl's output for
  the same reference art — turns "stays compatible" into a verified invariant
  without coupling the code.
- **Supersedes A8's subprocess + PIXELS→PNG bridge**; **reverses cross-doc
  decision §5.1** (CPC encoding now in-datagen Perl, not a subprocess — fine now
  the encoder is JSP's own Perl, not cpctelera's C/asm). To be recorded in
  README §5.1 / §5.13b + assets.md A8.
- Gating piece for **R9** (clone minimal_jsp→minimal_cpc; animated hero on CPC).
- **#2 heavy-refactor depth → DECISION: option A (2026-06-06).** Do **Stage 0 +
  Stage 1 now**, **defer Stage 2** to a separate, incremental future pass.
  - **Stage 0** (cheap leaf modules, byte-identical): e.g.
    `RAGE::Datagen::ColorTokens` (colour vocab + `_cpc_fw_color_for` +
    `_parse_zx_attr_tokens`).
  - **Stage 1** (the task's core — the asset-backend seam): `RAGE::AssetBackend`
    (registry/interface) + `RAGE::AssetBackend::ZX` (move `pixels_to_byte` + the
    ZX pixel/mask C-array emission out of `validate_and_compile_*` /
    `generate_{sprite,btile}` — **byte-identical**) + `RAGE::AssetBackend::CPCMode1`
    + `RAGE::CPCGfx` (packing copied/adapted from cpcgfx.pl) + the byte-compat
    test. datagen selects the backend by platform.
  - **Stage 2 — DEFERRED** to a separate incremental pass; tracked as **Task 6**
    below. Not part of Task 5.

### Status (carried from 2026-06-06 session)

- Stage 0 + Stage 1 landed (76edb9d/07ab4b9/d03e086): `RAGE::CPCGfx` +
  `RAGE::AssetBackend{,::ZX,::CPCMode1}`; datagen sprite+btile emission go
  through the backend. ZX byte-identical; `make all-test-builds` rc=0 (18 games);
  `perl tests/datagen/cpcgfx_pack.t` 6/6.
- DONE: the **byte-compat SPRITE test** — `tests/datagen/cpcgfx_sprite_compat.t`
  renders one 16x16 reference art (fg/bg/transparent) to a PNG and feeds it to
  BOTH packers: `cpcgfx.pl -g sprite_mask --extra-top-rows --extra-bottom-row`
  (and `-g tile`) vs `RAGE::CPCGfx::sprite_frame_bytes`/`tile_bytes` on the
  matching ASCII grids; asserts the emitted byte streams are byte-identical
  (sprite = 208 bytes). 4/4 pass. Turns the "datagen CPC bytes stay
  JSP-compatible" contract into a verified invariant. (Independently confirms
  R9's hero data was correct — the bug was cols sizing, not the bytes.)
- Stage 0+1 + byte-compat test complete → Task 5 done. Stage 2 stays Task 6.

## Task 6: datagen full modularization (DEFERRED — future work)

Deferred Stage 2 of the datagen refactor (decision A, Task 5). Not part of Task 5;
do it as a separate, incremental pass once Task 5's asset-backend seam has shipped.

- Split the monolith (`tools/datagen.pl`, ~5540 lines) into `RAGE::Datagen::*`
  modules: the 1229-line `read_input_data` parser, the ~1500 lines of
  `generate_*` emitters, flow-rules, datasets/codesets, tracker.
- **Prerequisite:** introduce a **context object** to replace the ~30 file-scoped
  globals (e.g. `@h_game_data_lines` — 87 refs; the `@all_*` model arrays + their
  `*_name_to_index` hashes; the emit accumulators). Without it, extracted modules
  just reach back into `main::` and nothing is actually decoupled.
- **Method:** one module at a time, **each extraction gated on `make
  all-test-builds` + `tests/00regression/` green and ZX byte-identical**. NOT a
  big-bang rewrite (avoid large untested modifications).
- Modules under `lib/RAGE/` (RAGE1 convention), per Task 5 decision #1.

- Work in a new "datagen_full_modularization" branch
- Use ultrathink

### Plan + progress (2026-06-07, branch datagen_full_modularization)

- Structural analysis done (datagen.pl = 5148 lines, 68 subs, ~30 file-scoped
  globals). Designed: a `RAGE::Datagen::Context` blessed hashref holding the model
  arrays + name→index hashes + accumulators (`@h_game_data_lines` etc.), introduced
  via a TEMPORARY glob-aliasing scaffold (`*main::all_btiles = $ctx->{all_btiles}`)
  so each sub extraction is mechanical, byte-preserving and reversible.
- Leaf-first extraction order (each gated, byte-identical), PARSER LAST:
  0 Context+scaffold → 1 ColorTokens → 2 Util(pixels_to_byte/optional_hex_decode/
  integer_in_range) → 3 PngDispatch → 4 BuildFeatures → 5 Validate → 6 Sprites →
  7 Btiles → 8 Screens → 9 FlowRules(dedup — highest byte-risk) → 10 Hero/Entities
  → 11 Codesets/Tracker/GameConfig → 12 Dependencies → 13 Parser (read_input_data).
- **Byte-identity gate = the `.asm` + features.h + DETERMINISTIC `.c`** (run
  `PERL_HASH_SEED=0 PERL_PERTURB_KEYS=0`): plain `.c` differs run-to-run via
  btile_deduplicate_arena_best hash-ordering (pre-existing non-determinism — proven
  by 2 unchanged-code runs diffing). Fast datagen-only gate: `make config data
  target_game=games/X`.
- **Step 1 DONE: `RAGE::Datagen::ColorTokens`** (resolve_color_tokens + the FG_/BG_
  table, the only pure colour-token leaf; 3 call sites). Imported via `use ... qw()`.
  Verified BYTE-NEUTRAL: deterministic PRE==AFTER for default/monochrome/crumbs;
  asm+features.h identical (4 games); minimal fully byte-identical; perl -c OK;
  unit check (FG_WHITE→INK_WHITE, INK_5 passthrough). all-test-builds gate running.
- This is a multi-session refactor; Step 1 committed as the first verified increment.
  Continue at Step 2 (Util) next session, same gated method. NOT pushed.

## Task 7: extension of CPC modes

- Design a plan extension (add a Phase 7?) for adding CPC-specific features:
  - Available CPC video modes: MODE0, MODE2
  - Palette specification (default, change-palette rules)
  - Generate additional demos for different CPC video modes and palette rules
  - Update documentation for all new features
- Just generate a new plan Phase, do not work on code yet.
- Add Phase 7 to the tasklist

### Landscape research (2026-06-07)

- JSP already supports all 7 CPC modes at compile time (jsp_cpc_geom.inc); RAGE1's
  gfx_jsp.h constants + RAGE::CPCGfx packing are already mode-parametric; plan docs
  reserved Mode 0/2 "for later without API change". Mode-1-only seams: JSP_CPC_CFLAGS
  `-DCPC_MODE1`, gfx_init hardcoded mode+pens, AssetBackend→CPCMode1, CPCGfx 2-colour
  only. CPC_PALETTE/CPC_COLOR_MAP parsed but inert. No RAGE1-level CPC regression yet.

### Completion (2026-06-07) — DRAFTED, FOR USER REVIEW (tomorrow)

- Phase 7 plan written to **doc/multiplatform-plan/cpc-modes.md** (goal, leverage,
  PROPOSED decisions D1–D7, staged sub-phases CM1–CM7 with gates, invariants, and
  §6 open questions Q-A..Q-F). NO code changed (per task).
- **Phase 7 block (CM1–CM7) added to management/00tasklist.md.**
- Decisions are PROPOSED — for the user to confirm/adjust. Status stays [~] pending
  that review; do NOT mark [x] until the user confirms the plan.

## Task completion status

- [x] Task 1: debug movement in CPC (PARKED — superseded by Phase 4J / R9 1px JSP movement)
- [x] Task 4: execute Phase 4J — JSP-CPC backend bring-up (R6/G10/A8/R9/R10 complete, merged)
- [x] Task 5: datagen refactor for multiplatform (Stage 0+1 + byte-compat test done)
- [~] Task 6: datagen full modularization (Stage 2 — context object + incremental extraction)
- [~] Task 7: extension of CPC modes (MODE0/MODE2 + palette) — plan/design
