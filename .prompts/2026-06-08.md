# Tasks

<!--
==============================================================================
HOW TO HANDLE THIS FILE (daily-tasks skill rules — read before every edit)
==============================================================================
- ON START of a task ("do task N"): the FIRST action, before any other work,
  is to flip that task's line in "## Task completion status" to [~]. If you
  catch yourself working without having flipped it, stop and flip it now.
- WHILE WORKING: record details/findings/completion notes as bullets in that
  task's own "## Task N" section (add a "### Completion notes" sub-heading if
  helpful). NEVER put prose in the status section.
- ON FINISH: tell the user, ask for confirmation. Do NOT mark [x] yourself.
  Status stays [~] until the user explicitly confirms.
- ON CONFIRMATION: only then change [~] -> [x]. User has the last word.
- STATUS MARKS: [ ] not started | [~] ongoing | [x] done (user-confirmed only).
- "## Task completion status" holds ONLY one-line "- [<mark>] Task N: <title>"
  entries. No prose, no sub-headings, no bullets there.
- Titles/details: terse, append-only — never reword the user's original text.
==============================================================================
-->

## Task 1: Start banking implementation

- Start the cpc-banked (CPC 6128) bring-up — Phase 4 remaining B6/B7/T3.
- ALL design decisions DC1–DC7 are resolved; full plan + gated step sequence in
  `doc/multiplatform-plan/management/cpc-banked-bringup.md` (canonical design in
  `banking.md` §6/§7 + `toolchain.md` Phase T3).
- Begin with **Stage T3a** (make a cpc-banked build EXIST, before engine infra):
  - T3-1/T3-2: `Makefile-cpc-banked` (model on `Makefile-zx128`, `ZCC_TARGET=+cpc`)
    + `zpragma-cpc-banked.inc` (org/stack per DC3).
  - T3-3/B6-6: `mmap-cpc-banked.inc` (DC3 numbers).
  - T3-8 + plumbing: `build-cpc6128` top target + `build-cpc` alias;
    `PLATFORM=cpc6128`→cpc-banked mapping (detect-platform.sh + resolver);
    `datagen -p cpc6128` emits `BUILD_FEATURE_PLATFORM_CPC_BANKED` (keep legacy macros).
- Build **shape-parametric** to experiment with the two memory-map partitions
  (DC3-D: Shape A = dataset buffer in page C; Shape B = full-page-A buffer, code in C).
- Work in branch **cpc_banked_bringup** (already off refactor_for_multiplatform,
  rebased onto trunk incl. Task 7 + Phase 8 design). NOT pushed.
- Gated per step: build green + ZX byte-identical (existing 18 games) + cap32 check
  where runnable + independent reviewer before commit. Early checks: verify z88dk
  `+cpc` accepts `CRT_ORG_CODE=0x8000` (Shape B) + AMSDOS `RUN"` load at that org.

### Progress (live)
- T3a increment 1 (T3-7) DONE: datagen `cpc6128` support. Parser.pm accepts
  `PLATFORM cpc6128` → emits `BUILD_FEATURE_PLATFORM_CPC6128` (machine id) +
  `PLATFORM_CPC_BANKED` (memory model) + INPUT_BACKEND_CPC (mirrors cpc464→cpc-flat);
  datagen.pl `-p cpc6128` accepted (forced_build_target=0). Verified: syntax OK;
  smoke (minimal cpc6128 gdata) emits the two PLATFORM_CPC* macros; bad `-p` rejected;
  ZX path unregressed (`-p zx48` on games/minimal still emits PLATFORM_ZX48/ZX_TARGET_48).
  Purely additive — ZX byte-identical by construction.
- T3a increment 2 IMPLEMENTED: new `zpragma-cpc-banked.inc` + `mmap-cpc-banked.inc`
  (Shape-A constants, copies of the cpc-flat ones) + `Makefile-cpc-banked` (copy of
  Makefile-cpc-flat, repointed MYMAKE/ZPRAGMA/MMAP, no banking yet) + top-Makefile
  `build-cpc6128`/`build-cpc`/`data-cpc6128`/`build-cpc-hello-banked` + .PHONY/help +
  detect-platform.sh cpc6128 + new game `games/cpc-hello-banked/` (PLATFORM cpc6128).
  - VERIFIED: `make build-cpc6128 target_game=games/cpc-hello-banked` EXIT 0 → game.dsk
    (194 KB) + game.cpc; features.h has PLATFORM_CPC6128 + PLATFORM_CPC_BANKED;
    section-check OK; **cap32 headless boot screenshot shows it running** (all hello lines).
  - Independent review: REQUEST CHANGES → fixes applied:
    - BLOCKER B1 (FIXED): `Makefile-cpc-banked` was missing from `SPLIT_COPY_ITEMS`
      (Task 7 split-dir copy set) → the PARALLEL test of cpc-hello-banked couldn't find
      its Makefile (masked by the repo-root single-game build). Added it.
    - MINOR M1 (FIXED): swept stale cpc-flat / Phase-T2 / wrong-zpragma-file comments +
      echoes in Makefile-cpc-banked (the fresh copy made false phase/platform claims).
  - Full matrix AFTER B1 fix = 19/19 (14 ZX + 5 CPC incl. cpc-hello-banked), EXIT=0,
    10:33. B1 fix confirmed (cpc-hello-banked builds in the parallel split path); ZX
    no-regression; all CPC green. ZX byte-identity holds by construction (only additive
    surface; no ZX/cpc-flat codepath touched). T3a milestone gate MET.
- NEXT after T3a green+commit: Phase B6 (engine banking infra) — bank-switch primitive
  (direct MMR out, Configs 4-7), per-platform BANKED_* macros, ISR, cpc-banked/ dir.
- **Stage B6 STARTED.** Step 4 (B6-3/B6-4 — per-platform banking macros + make the
  banking-consumer engine files compile under cpc-banked) IMPLEMENTED:
  - Macros (Shape A, mirroring the ZX128 hardcode-with-YAML-comment pattern):
    memory.h `ENGINE_CODE_MEMORY_BANK=4` / `BANKED_FUNCTION_TABLE_BASE=0x4000` /
    `DATASET_LOAD_BASE=0x4000`; codeset.h `CODESET_ASSETS_BASE=0x4000`; dataset.h
    `BANKED_DATASET_BASE_ADDRESS=0x8000` (cpc-banked) else `0x5B00` (ZX kept byte-id).
  - cpc-banked "is a CPC" guard misses fixed (were keyed on CPC464||CPC_FLAT, missed
    cpc6128/cpc_banked): platform.h (arch/spectrum.h shim), input.h (input_cpc.h HAL
    stub), memory.c (BSS heap reservation + skip 2-arg heap_init). 00bswitch.c bank-
    switch PRIMITIVE deliberately deferred to step 5 (still ZX-only `_IO_7FFD` asm).
  - data-cpc6128 now also runs `generate_banked_function_defs.pl -p cpc-banked` (banked
    platform needs banked_function_defs.h; org from banking.cpc-banked.swap_window=0x4000).
  - NEW durable gate: `games/00cpc-banked-compile-test/` (cpc6128 sibling of
    00cpc-compile-test) + `make build-00cpc-banked-compile-test`; auto-joins the matrix
    via the `00cpc%` filter. Compiles whole engine MINUS 00bswitch.c (filtered in
    Makefile-cpc-banked until the CPC primitive lands — REMOVE filter at step 5).
  - VERIFIED: build-00cpc-banked-compile-test EXIT 0 (engine type-checks under +cpc with
    PLATFORM_CPC_BANKED active). ZX/cpc-flat byte-identical by construction (additive
    `#ifdef CPC_BANKED` arms + guard-widenings inert on ZX/cpc-flat). Full all-test-builds
    matrix: **20/20 green** (14 ZX unregressed + 6 CPC incl. the new 00cpc-banked-compile-test;
    cpc-flat 00cpc-compile-test / minimal_cpc unregressed). 12:53.
- **Step 5 (B6-1/B6-2 — CPC bank-switch primitive) DONE.** 00bswitch.c: added the
  cpc-banked arm of `memory_switch_bank()` — same atomic DI/out/EI +
  interrupt_nesting_level interlock as the ZX128 version (now wrapped in `#ifdef ZX128`,
  bytes unchanged → byte-identical), only the HW write differs: `or a,0xC0` (GA RAMR
  command) + `ld bc,0x7F00` (GA port 0x7F) + `out (c),a`. bank->Config is the identity
  map (config=bank for {0,4,5,6,7}; Config 3 excluded by the YAML bank lists = screen).
  Removed the 00bswitch.c filter from the cpc-banked compile-test (whole engine now
  type-checks). Opcodes disassembled & confirmed (f6 c0 / 01 00 7f / ed 79). Caught+fixed
  a preprocessor bug: an apostrophe in a `;;` asm comment ("don't-care") broke cpp
  (truncated token / unterminated #if).
  - VERIFIED: build-00cpc-banked-compile-test EXIT 0; all-test-builds **20/20** (14 ZX
    byte-id + 6 CPC). Independent review: **APPROVE** (HW encoding + atomicity both
    confirmed correct; 2 cosmetic NITs, 1 applied).
  - DEFERRED (correctly): memory_current_memory_bank=0 init for cpc-banked + widening the
    `ZX_TARGET_128`-gated banking RUNTIME guards (already a recorded tracker Follow-up);
    runtime MMR validation on cap32 lands with games/cpc-bswitch-test (step 7).
- **Primitive EMPIRICALLY VALIDATED on cap32 (128K 6128).** Built games/cpc-bswitch-test
  (cpc6128, own main(), links ONLY 00bswitch.c via CPC_LINK_BSWITCH=1; supplies
  interrupt_nesting_level locally). The probe stamps a distinct marker into each
  expansion bank {4,5,6,7} through the 0x4000 window, returns home, reads each back:
  cap32 shows RAM 4/5/6/7 wrote/read 44/55/66/77 all OK -> **RESULT: PASS**. The
  Gate-Array RAM-config select works exactly as designed. Evidence committed:
  games/cpc-bswitch-test/cap32-pass.png. New target build-cpc-bswitch-test; game
  auto-joins the matrix via the cpc-% filter.
  - VERIFIED: all-test-builds **21/21** (14 ZX + 7 CPC). Independent review: APPROVE
    (probe soundness, safety, link wiring all confirmed; 2 cosmetic NITs, left as-is).
- **Step 6 (B6-8 — CPC IM1 ISR) DONE + validated.** No new ISR code needed: the CPC IM1
  path (300 Hz fast handler via cpc_add_fast_isr + divide-by-six -> 50 Hz do_timer_tick) was
  already written generically for CPC464||CPC6128 by G8 and compiles for cpc-banked;
  CRT_DISABLE_FIRMWARE_ISR=1 is set in zpragma-cpc-banked.inc. Per banking.md §3.5 the ISR
  lives in page A (always mapped) — trivially true now (all code in page A); the §3.5.1
  banked interlock (Rule C: ISR paging banked audio) is deferred (only with banked audio).
  - VALIDATED on cap32: built games/cpc-isr-test (links interrupts.c + asmdata_cpc.c via
    CPC_LINK_ISR=1; needs GFX_BACKEND JSP so interrupts.c's debug.h->gfx.h->gfx_sp1.h chain
    compiles on CPC). init_interrupts() then current_time advances: ticks 0->86, secs 0->1
    over ~1.7s -> **ISR 50Hz TICK: PASS** (divide-by-six confirmed). Evidence:
    games/cpc-isr-test/cap32-pass.png.
  - all-test-builds **22/22** (14 ZX + 8 CPC). Independent review: APPROVE-WITH-NITS (PASS
    trustworthy/no false-positive path, link-safe; comment fixed; debug.h->gfx.h decoupling
    recorded as a follow-up).
  - NEXT: step 7 (B6-5/B6-7) engine/banked_code/cpc-banked/ dir mirroring 128/, set
    BANKED_CODE_DIR_<tag> in Makefile-cpc-banked -> B6 stage gate.
- **Step 7 (B6-5/B6-7 — banked_code/cpc-banked/ dir + make plumbing) DONE.** Commit
  cfddf12. Structural mirror of engine/banked_code/128/ for cpc-banked (DC3-A): new dir
  engine/banked_code/cpc-banked/ (README placeholder, git-tracked) + Makefile.common
  GENERATED_DIR_BANKED_CPC_BANKED + BANKED_CODE_{DIR,ASMSRC,CSRC,SRC,OBJS}_CPC_BANKED
  (faithful mirror of the _128 set) + Makefile-cpc-banked BANKED_CODE_{DIR,SRC,OBJS}_PLATFORM
  selectors (B7 banked-code target will consume them; INERT at B6 — referenced by no target)
  + top Makefile clean-glob for cpc-banked artifacts. Real banked compilation/link is B7/step 8.
  - VERIFIED: build-00cpc-banked-compile-test EXIT 0; all-test-builds **22/22** (14 ZX + 8
    CPC). ZX/cpc-flat byte-identical by construction (new vars consumed by no existing target;
    reviewer gr-proved inertness). Independent review: **APPROVE** (no blockers; 2 NITs on the
    pre-existing `,,` empty-brace clean glob, left as-is for consistency with _128/_COMMON).
- **STAGE B6 COMPLETE — exit gate MET.** Engine compiles under cpc-banked with banking on
  (00cpc-banked-compile-test); bank-switch primitive + IM1 ISR EMPIRICALLY VALIDATED on cap32
  (cpc-bswitch-test + cpc-isr-test PASS); ZX byte-identical held every step. NEXT = **Stage B7**
  (steps 8-10): banktool --platform + real bank binaries (step 8), asmloader template +
  loadertool --platform (step 9), games/cpc-banked-test 1-dataset/1-codeset smoke (step 10).
- **STAGE B7 STARTED. Step 8 (B7-1/T3-5 — banktool.pl --platform) DONE.** banktool.pl now
  derives its usable bank set from per-platform YAML (`banking.<platform>.{dataset,codeset}_
  valid_banks` + `engine_code_memory_bank`) instead of the hard-coded ZX `{1,3,4,6,7}`:
  zx128 reproduces `{1,3,4,6,7}` (bank 4 reserved) BYTE-IDENTICALLY; cpc-banked yields
  `{4,5,6,7}` (RAM 4 reserved for engine code, datasets pack after it; codeset→bank 5).
  Also fixed a config nit (reviewer): cpc-banked `codeset_valid_banks` `[5,6,7,4]`→`[5,6,7]`
  (engine bank 4 must not be in codeset list per the documented invariant; mirrors zx128;
  functionally a no-op since banktool always skips the full reserved bank). banktool emits
  bank#+RELATIVE-offset only → no `0x8000` handling needed here (loader/engine concern, step 9).
  - VERIFIED: ZX byte-identity PROVEN via fixed-seed (`PERL_HASH_SEED=0 PERL_PERTURB_KEYS=0`)
    controlled run — HEAD vs new banktool on identical inputs → bank_*.bin / dataset_info.asm /
    bank_bins.cfg all byte-identical (the real-build bank_1.bin/dataset_info.asm run-to-run
    diffs are PRE-EXISTING non-determinism from non-reproducible banked_code.bin size feeding
    the layout search — confirmed present with HEAD banktool too). cpc-banked derivation smoke-
    tested (bank set {4,5,6,7}, bank 4 RESERVED+datasets, bank 5 codeset). all-test-builds
    **22/22** (14 ZX + 8 CPC). Independent review (Perl+RAGE1): **APPROVE-WITH-NITS** (no
    blockers; `//` vs `||` confirmed correct; codeset-list nit fixed).
  - NOT yet wired into a cpc-banked BUILD: no cpc-banked banked-code compile target nor a
    cpc-banked game-with-banks exists yet (banktool's cpc-banked path is dormant until the
    asmloader (step 9) + games/cpc-banked-test (step 10) land). NEXT = step 9.
- **NEW step 8a inserted (gates step 9): cpc-banked disk-loader design.** User raised the
  memory-tight requirement: disc images up to ~128 KB, banks stay ALREADY-COMPRESSED (no
  load-time decompress), payload exceeds the 64 KB address space -> the loader MUST STREAM
  bank data from disc bank-by-bank into the 0x4000 window (<=16 KB resident at a time) via
  AMSDOS firmware file I/O (CAS IN DIRECT into a paged Config-4..7 expansion bank). Design
  note written: `doc/multiplatform-plan/cpc-banked-disk-loader.md` (DRAFT, under review).
  Plan: per-bank files + firmware CAS IN DIRECT, PoC-FIRST — `games/cpc-disk-bankload-test`
  must validate firmware-load-into-a-paged-bank on cap32 (risk R-DL1) + settle multi-file DSK
  packaging (R-DL2; investigate `appmake --bankspace`) BEFORE any loader/template code. Awaiting
  user review of the design note.
- **Design note REVIEWED + APPROVED (user, 2026-06-08)**: per-bank files + firmware CAS IN DIRECT,
  PoC-first, no preemptive firmware-independent fallback; defer DSK tooling; R-DL3 resolved (AMSDOS
  workspace = JSP-buffer region, unused pre-game); firmware-disable stays with init_interrupts().
- **STEP 8a DONE — R-DL1 EMPIRICALLY VALIDATED on cap32.** PoC `tools/cpc-bankload-poc/` (plain +cpc,
  org 0x1200 page A): zero RAM5 -> page RAM5 @ 0x4000 -> firmware CAS IN DIRECT loads disc file into
  the window -> restore Config 0 -> re-page RAM5 -> find embedded 8-byte signature -> **PASS** (solid
  screen, evidence cap32-pass.png). **Negative control** (skip only the load) -> signature absent ->
  blank FAIL, proving the PASS was caused by the firmware writing the paged bank. So firmware file I/O
  DOES transfer into the 0x4000 window with an expansion bank mapped there — the design's load-bearing
  assumption holds.
  - **Firmware-call finding** (durable, see design note §6): a direct `call 0xBC77` crashes because the
    z88dk +cpc C CRT seizes the firmware's alternate-register set + 0x0038 ISR at startup; the PoC routes
    calls through z88dk's `firmware` interposer. The REAL asmloader is pure ASM at cold boot (native
    firmware state, pre-CRT) so it won't need the interposer — but must do file I/O before `jp main`.
  - **R-DL2 resolved**: `appmake +cpc --disk` already writes a multi-file `.bN`-per-bank bootable EDSK
    (z88dk banked-linker driven); step 9 bridges RAGE1's banktool `bank_*.bin` to that EDSK writer. No
    new DSK tool needed. NEXT = step 9 (asmloader templates + loadertool --platform=cpc-banked).

## Task completion status

- [~] Task 1: Start banking implementation
