# TASKS

## Task 1: adjust memory tracking after spritelib refactor

- The `make mem` target shows memory usage for both 48 and 128 modes, and is currently hardcoded for SP1 memory usage. Analyze the JSP design document again (changes done recently), and create new scripts and targets that take into account the new sprite library.
- Don't make the scripts smarter. The scripts for memory usage must be specific for each spritelib. Make the Makefile targets smart and select the proper script based on the TARGET and SPRITELIB settings in the current game

Done:
 - 4 per-spritelib scripts: tools/mem-summary-{sp1,jsp}-{48,128}.sh — each
    hardcoded for one spritelib+target, no detection logic inside
  - Makefile `mem` target selects the script via
    $(BUILD_SPRITE_ENGINE)-$(ZX_TARGET)
  - removed obsolete tools/mem-summary{,-48,-128}.sh (dispatcher + in-script
    detection)
  - jsp-128 needed a corrected memory model (the inherited JSP-128 logic was
    structurally wrong — it modelled JSP sprite data as the topmost chunk
    like SP1). JSP 128K actually embeds jspdata ($a240-$bfff, fixed bank 2)
    and the IV/stack region *within* the resident binary's address span;
    code continues above jspdata into the home bank up to $ffff. Fixed:
    intstk = iv_table..jspdata-1; TOTAL = $4000..code_tail; FREE = gap above
    the code (no longer adds jspdata separately or caps at $c000)
  - JSP scripts note the 1536 B ex-DRT hole freed by the recompositing redesign
  - verified: minimal (sp1/48), minimal_jsp (jsp/48), sub_bufs_128 (sp1/128),
    default_jsp (jsp/128) — default_jsp reports 11632 B free and fits fine
  - independent review of all 4 scripts: PASS
  - added `make mem-compare` (tools/mem-compare.sh): builds games/default
    (SP1) and games/default_jsp (JSP) — same 128K game bar the sprite engine
    — prints both mem reports + a per-section SP1-vs-JSP delta. Result: JSP
    uses 9796 B less than SP1 for the default game (SP1 1836 B free vs JSP
    11632 B free)
  - fixed pre-existing screen end-address bug ($582f -> $5aff) in the 128
    scripts (flagged by the review)

## Task Completion Status:

- [x] Task 1: memory tracking adjusted for the sprite-library refactor
 